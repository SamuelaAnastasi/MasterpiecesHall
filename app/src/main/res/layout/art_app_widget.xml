<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/widget_margin">

    <ImageView
        android:id="@+id/widgetIv"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@string/cnt_desc_widget_image"
        android:scaleType="centerCrop"
        android:src="@drawable/web_image_2" />

    <TextView
        android:id="@+id/appwidget_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="end|bottom"
        android:background="@color/transparent_black"
        android:contentDescription="@string/appwidget_text"
        android:ellipsize="end"
        android:fontFamily="@font/dosis_regular"
        android:maxLines="2"
        android:minLines="2"
        android:padding="@dimen/small_title_padding"
        android:textColor="@color/colorWhite"
        android:textSize="@dimen/small_title_size"
        tools:text="Portrait of a Couple, Probably Isaac Abrahamsz Massa and Beatrix van der Laen, Frans Hals, c. 1622" />

</FrameLayout>